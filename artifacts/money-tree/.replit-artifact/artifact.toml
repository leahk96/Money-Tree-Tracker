kind = "web"
previewPath = "/"
title = "Money Tree"
version = "1.0.0"
id = "artifacts/money-tree"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18638

[services.development]
run = "pnpm --filter @workspace/money-tree run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/money-tree", "run", "build" ]
publicDir = "artifacts/money-tree/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18638"
BASE_PATH = "/"
